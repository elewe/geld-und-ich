# Components

Dieser Ordner enthält alle wiederverwendbaren React-Komponenten für Kids Money Lab.

## Struktur

- `ui/` - UI-Komponenten (Buttons, Cards, Inputs, etc.)
- `layout/` - Layout-Komponenten (Header, Footer, Sidebar, etc.)
- `features/` - Feature-spezifische Komponenten





